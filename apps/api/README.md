# apps/api — Scout FastAPI service

This service runs the JSON API, serves the built React SPA from `static/`,
and hosts the in-process APScheduler that runs background jobs (scraping,
embedding, matching, decay, digests).

Filled in step 06 of [`/PLANS/phase-1/`](../../PLANS/phase-1/).
